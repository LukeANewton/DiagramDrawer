package diagramdrawer.model.connectiontype;

import javafx.scene.canvas.GraphicsContext;
import javafx.scene.paint.Color;

/**Describes how to draw the head on an association relationship*/
public class Association implements ConnectionHead {
    @Override
    public void drawHead(GraphicsContext gc, Color color, int lineWidth) {

    }
}
